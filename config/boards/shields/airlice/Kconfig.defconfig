if SHIELD_AIRLICE_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "airlice"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_AIRLICE_LEFT || SHIELD_AIRLICE_RIGHT

config ZMK_SPLIT
    default y

endif